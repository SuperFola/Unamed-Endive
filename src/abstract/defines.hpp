#ifndef GUARD_DEFINES
#define GUARD_DEFINES

#define setupFont( text , font , color , chsize ) \
              text.setFont(font); \
              text.setFillColor(color); \
              text.setCharacterSize(chsize);

#endif // GUARD_DEFINES
