#ifndef DEF_TYPE
#define DEF_TYPE

enum Type {
    NORMAL
};

enum SortilegeType {
    UniqueTargetAdv,
    UniqueTargetUs,
    MultipleAdv,
    MultipleUs
};

enum ActionType {
    Equipment = 0,
    OnCrea = 1
};

#endif // DEF_TYPE
