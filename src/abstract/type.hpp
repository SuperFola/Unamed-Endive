#ifndef DEF_TYPE
#define DEF_TYPE

enum Type {
    NORMAL
};

enum SortilegeType {
    UniqueTargetAdv,
    UniqueTargetUs,
    MultipleAdv,
    MultipleUs
};

enum ActionType {
    Equipment,
    OnCrea
};

#endif // DEF_TYPE
